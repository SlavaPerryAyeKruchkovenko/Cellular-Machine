<UserControl
    x:Class="Views.MachineCanvasView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
  <ItemsControl Items="{Binding Cells}">
    <ItemsControl.ItemsPanel>
      <ItemsPanelTemplate>
        <Canvas Classes="Purple" PointerPressed="AddRectangle" x:Name="holst"/>
      </ItemsPanelTemplate>
    </ItemsControl.ItemsPanel>
    <ItemsControl.ItemContainerStyle>
      <Style Selector="Rectangle">
        <Setter Property="Canvas.Left" Value="{Binding Path=Location.X}"/>
        <Setter Property="Canvas.Top" Value="{Binding Path=Location.Y}"/>
      </Style>
    </ItemsControl.ItemContainerStyle>
    <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Rectangle Width="{Binding Path=Size.Width}" Height="{Binding Path=Size.Height}"
                         Fill="Yellow" Tapped="DeleteRect"
                         ContextFlyout="{Binding Path=Neighbors}"
                           >
              </Rectangle>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
  </ItemsControl>
</UserControl>
